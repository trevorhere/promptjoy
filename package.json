{
  "name": "promptjoy",
  "private": true,
  "scripts": {
    "deploy:contact-worker": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply promptjoy --local",
    "db:migrate:remote": "wrangler d1 migrations apply promptjoy --remote"
  },
  "devDependencies": {
    "wrangler": "^4.59.2"
  }
}
